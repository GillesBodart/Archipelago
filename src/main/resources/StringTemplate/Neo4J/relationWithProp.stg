RelationNeo4J(idA,idB,name,properties) ::= <<

    MATCH (n) where ID(n) = <idA>
    MATCH (m) where ID(m) = <idB>
    CREATE (n)\-[r:<name> {<properties : property(); separator=", ">}]->(m) return r
>>

property(prop) ::= "<prop> : {<prop>}"